import { LetraApellidoPipe } from './letra-apellido.pipe';

describe('LetraApellidoPipe', () => {
  it('create an instance', () => {
    const pipe = new LetraApellidoPipe();
    expect(pipe).toBeTruthy();
  });
});
